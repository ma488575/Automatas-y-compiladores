%option noyywrap
%{
	#include<stdio.h>
%}
TOKEN_NUMERO [0-9]+
TPKEN_DECIMAL [0-9]+[.][0-9]+
%%
{TOKEN_NUMERO} { printf("TOKEN_NUMERO=%s\n", yytext);}
{TOKEN_DECIMAL} { printf("TOKEN_DECIMAL=%s\n", yytext);}
%%
int main()
{
        yylex();
	return 0;
}